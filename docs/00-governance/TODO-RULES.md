# TODO Rules

## 1. Purpose

本文档定义根目录 `TODO.md` 的任务格式、协作用法和删除规则。

目标是让 `TODO.md` 只承载未关闭任务队列，不承载方案正文、完成历史、长篇论证或未经审阅的执行许可。

## 2. Scope

当前范围：

- 根目录 `TODO.md`
- 宏观任务与执行任务的边界
- 任务拆解、人机审阅和执行许可
- `TODO.md` 的固定格式
- 任务完成后的删除、拆分和收窄
- 任务收口时的测试、文档和提交检查

不在范围内：

- 不替代具体业务需求文档
- 不替代架构红线或实现规则
- 不替代测试写法教程
- 不替代 Git 基础使用教程
- 不记录已完成任务历史

## 3. Bounded Context

`TODO.md` 是未关闭任务面板。

本仓库把“任务完成”定义为一个闭环，而不是单纯代码已改完。代码、测试、文档、`TODO.md` 清理和提交必须收敛成同一条可解释的变更链路。

`TODO.md` 固定只回答：

1. 当前还有什么未关闭任务。
2. 每个任务的处理动作是什么。
3. 每个任务完成时如何判断。

完整方案、规则背景和执行细节必须放在对应治理文档、`HOW-TO` 或专项设计文档中。`TODO.md` 只引用这些文档，不复制这些文档。

## 4. TODO Types

`TODO.md` 承载两类任务：

- 宏观任务：已经值得讨论，但尚未拆解成执行步骤。
- 执行任务：已经达成共识，或已经拆解完成并等待人工审阅。

宏观任务不是执行许可。拆解并经人工审阅后的执行任务，才是执行入口。

## 5. File Structure

`TODO.md` 固定使用以下结构：

```markdown
# TODO List

## 说明

- `TODO.md` 是未关闭任务面板，不是完成历史。
- 宏观任务必须先讨论边界，再拆解为可执行 TODO。
- 已完成任务必须删除，不在 `TODO.md` 中打勾长期保留。
- 完成历史保留在 commit 或 PR 中。

## 当前任务项

- [ ] `范围对象`：任务标题
  - 范围文件：精确文件路径，多个文件按行列出
  - 处理动作：一句话说明要做什么
  - 验收点：可检查的完成结果
  - 重要度：N/10

## 待审阅任务项

- [ ] `范围对象`：任务标题
  - 范围文件：精确文件路径，多个文件按行列出
  - 处理动作：一句话说明要做什么
  - 验收点：可检查的完成结果
  - 重要度：N/10

## 待讨论项

- [ ] 需要决策的问题
  - 关联任务：任务名或范围对象
  - 决策要求：需要人工确认的具体问题
  - 重要度：N/10
```

没有任务时，`当前任务项` 可以为空，但不得记录已完成历史。

## 6. Task Item Rules

执行任务固定使用 `- [ ]`。

每个执行任务必须包含：

- `范围文件`
- `处理动作`
- `验收点`
- `重要度`

规则：

- `范围对象` 必须能稳定定位业务域、模块或治理域。
- `范围文件` 必须写精确路径。
- 只有盘点任务允许写目录或搜索模式。
- `处理动作` 只写一个明确动作。
- `验收点` 只写可检查结果。
- `重要度` 使用 `N/10`。
- 单项任务文件过多时必须继续拆分。

不得在任务项中写：

- 完整方案
- 长篇背景
- 预设提交信息
- 长命令教程
- 已完成历史
- 与该项无关的后续步骤

## 7. Collaboration Rules

写入宏观任务前，固定判断：

1. 该任务是否已经足够重要，值得进入 `TODO.md`。
2. 该任务是否能用一句话说明问题或目标。
3. 该任务是否存在明确的下一步讨论点。
4. 该任务是否不是低置信度观察项。
5. 该任务是否不应直接进入执行。

拆解执行任务前，固定判断：

1. 宏观任务是否已经经过人机讨论。
2. 拆解后的 TODO 是否需要人工审阅。
3. 每个执行 TODO 是否能独立验收。
4. 每个执行 TODO 是否能对应到代码、测试、文档或提交动作。
5. 执行顺序是否合理。

未经人工确认的拆解任务固定放入 `待审阅任务项`。AI 不得执行 `待审阅任务项`。

## 8. Deletion Rules

完成任务后固定处理：

- 完成整项：删除该 TODO。
- 完成部分：收窄为剩余未完成内容。
- 发现拆分错误：重写为正确的待审阅任务。
- 待讨论项完成决策：删除待讨论项；如仍需执行，新增明确执行任务。

已完成任务不得在 `TODO.md` 中打勾长期保留。完成历史保留在 commit 或 PR 中。

删除已完成 TODO 项必须和完成该任务的代码、测试或文档修改放在同一个 commit。

## 9. Closure Rules

任务完成后，进入收口前固定先判断：

1. 当前任务的实现目标是否已经达到可运行、可检查状态。
2. 当前改动是否仍存在未完成的剩余范围。
3. 当前改动是否影响测试、文档或任务入口。
4. 当前工作区中是否混入了其他不相关修改。
5. 当前提交是否能够用一句明确的话解释“这次到底改了什么能力”。

只要以上任一问题不能明确回答，就不得进入收口。

收口时固定先判断 `TODO.md` 对应任务项是否真的完成：

- 任务目标已经完成，且没有剩余未实现范围：删除对应 `TODO.md` 项。
- 任务只完成一部分，但剩余范围仍清楚存在：不要删除；将任务拆分或收窄为剩余未完成内容。
- 当前只是局部验证通过，但需求或边界尚未稳定：不要删除；保持原任务项不动。

## 10. Test Check Rules

收口前必须检查当前改动影响了哪些测试层次。

最小检查清单：

- Controller 是否新增或改变了公开 HTTP 签名、校验、权限、页面跳转或异常响应。
- Service 是否改变了业务流程、事务边界、状态流转、跨 DAO 编排或结果模型。
- DAO / Mapper 是否改变了查询、分页、SQL、实体映射或持久化约束。
- static 是否改变了 API 文档、上传访问或其他运行支撑资源。
- 是否改动了架构规则、命名规则或其他可能影响静态检查的内容。

判断结果：

- 行为变化已发生：同步补或更新对应测试。
- 只有纯机械改名、注释、格式整理，且不影响行为和规则：可以不新增测试，但必须确认没有误触行为边界。

## 11. Document Sync Rules

收口前必须检查当前改动是否要求同步文档。

最小检查清单：

- 需求口径变化：检查 `10-requirements/*-REQUIREMENTS.md`。
- 治理规则或固定做法变化：检查 `docs/00-governance/*.md`。
- 数据库结构、字段、查询语义变化：检查 `20-database/*-DATABASE-DESIGN.md`。
- AI 默认读取入口或任务路由变化：检查 `docs/AGENT.md`。

判断原则：

- 改动影响文档定义的固定口径时，必须同步文档。
- 仅代码实现补齐、但不改变既有文档口径时，不为“看起来完整”而顺手改无关文档。

## 12. Commit Rules

commit 粒度固定先保证单个 commit 语义完整，再避免把不相关判断混在同一个 commit 中。

可以放在同一个 commit 的内容：

- 同一能力的代码、测试、文档同步。
- 对应 `TODO.md` 项删除、拆分或收窄。
- 为同一判断服务的命名或装配修正。

必须拆成多个 commit 的内容：

- 不同业务模块的独立能力改动。
- 同一任务中彼此无直接因果关系的两类判断。
- 纯治理补丁与业务功能改动，如果它们不是同一个闭环。

`commit message` 必须同时表达改动类型、业务域或治理域、具体能力变化。

本仓库固定格式：

`Type(domain): 中文说明`

检查清单：

- `Type` 是否准确表达这是 `Feat`、`Fix`、`Docs`、`Test`、`Refactor` 等哪类变化。
- `domain` 是否能稳定定位业务域、模块或治理域。
- 中文说明是否点出具体能力，不写空泛的“调整”“修改”“优化”。

## 13. Common Mistakes

- 把完整方案写进 `TODO.md`
- 把宏观任务伪装成可执行任务
- 人工尚未确认边界就开始执行
- 拆解后的 TODO 粒度过细，制造噪音
- 拆解后的 TODO 混入未经讨论的新目标
- 用模块名或目录名代替精确文件路径
- 在 TODO 中写预设提交信息和长命令教程
- 完成后在 `TODO.md` 保留打勾历史
- 把最后的现场清理混入第一批迁移任务
- 任务只完成一部分，就删除整个 `TODO.md` 项
- 代码行为变了，但因为“先能跑”而不补测试
- 规则口径已经变化，却没有同步需求文档或治理文档
- 多个不相关判断混在同一个 commit 中
- `commit message` 只写“调整”“修改”“优化”，看不出具体能力变化

## 14. Verification

修改 `TODO.md` 后固定检查：

1. 是否只有未关闭任务。
2. 是否没有完成历史。
3. 每个执行任务是否包含 `范围文件`、`处理动作`、`验收点` 和 `重要度`。
4. `待审阅任务项` 是否没有被描述成可立即执行。
5. 每项任务是否可以对应一个小提交。

收口前固定检查：

1. `TODO.md` 是否准确反映剩余未完成内容。
2. 本次改动影响到的最小测试层次是否已经检查。
3. 本次改动影响到的最小文档类型是否已经检查。
4. 当前工作区是否只包含准备提交的同一闭环修改。
5. 当前 commit 是否能被一句明确的话解释。

## 15. Related Documents

- 文档写作与维护见 [`DOCUMENT-RULES.md`](./DOCUMENT-RULES.md)

## 16. Open Items

无
