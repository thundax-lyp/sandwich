你将基于当前仓库的 Git 提交记录，自动生成一篇“项目演进复盘故事”。

该项目名为 **Sandwich**，这是一个**由 AI 持续生成与演化的项目**，因此本故事重点不是实现细节，而是**判断过程与方法形成过程**。

====================
【任务执行】
======

1. 读取当前仓库全部 commit short message（按时间顺序，从旧到新）
2. 分析：

    * 提交密度变化
    * commit 类型变化（init / feat / refactor / test / docs 等）
    * 连续行为（如一组 Outbox / 一组 ArchUnit / 一组回退）
3. 按“演进阶段”组织内容（必须基于提交模式变化，不允许平均切分）
4. 生成复盘故事
5. 写入文件：docs/60-human/STORY.md（覆盖写）

禁止输出任何 docs/60-human/STORY.md 之外的内容

---

====================
【文档开头（必须原样输出）】
==============

# Sandwich 项目故事

本文是 Sandwich 的演进叙事，不是实现规范。

* 不作为工程规则依据
* 不作为 AI 默认输入文档
* 不作为架构设计说明

只记录“这支团队（含 AI）如何判断、取舍、修正”。

文中所有判断，均基于 commit 行为的观察性推断，而非作者主观表达。

---

====================
【核心目标】
======

还原一条清晰主线：

系统如何从：

* 能跑
  → 可用
  → 可恢复
  → 可约束
  → 可持续演进

⚠️ 方法论不能直接总结，必须从过程自然显现

---

====================
【阶段划分规则（强约束）】
=============

必须基于以下信号划分阶段：

* 提交密度突变（爆发 / 收敛）
* commit 类型变化
* 关注点变化（功能 → 失败路径 → 语义 → 门禁 → 治理）
* 出现“连续动作组”（Outbox / Facade / ArchUnit / Tenant 等）

❌ 禁止：

* 平均按时间切分
* 固定阶段数量

---

====================
【每个阶段结构（必须包含）】
==============

1. 【触发点】

    * 描述“哪里开始不一样了”
    * 必须基于 commit 特征（密度 / 类型 / 主题）

2. 【关键动作】

    * 至少 2–3 类具体行为
    * 必须具体（如：幂等、死信、DTO 下沉、门禁规则）

3. 【冲突 / 反复（关键加分项）】

    * 如果存在：

        * 迁移 → 回退 → 重做
        * 规则 → 撤回 → 再收紧
    * 必须明确写出“这个阶段不稳定”

4. 【隐含判断（观察语气）】

    * ✅ “从这些提交看，更像是在……”
    * ✅ “这一变化通常意味着……”
    * ❌ 禁止写成“作者的意图是……”

---

====================
【故事张力要求（重点）】
============

必须至少识别并放大 2–3 个“关键波动点”，例如：

* Facade 来回迁移
* ArchUnit 门禁反复收紧/放松
* Tenant 语义大规模回退
* 大规模 Revert

对于这些点：

❌ 不要一句话带过
✅ 必须展开为“事件”，体现：

* 不确定性
* 判断未稳定
* 收敛过程

---

====================
【AI 特性（必须体现）】
=============

必须在 2–3 个阶段自然体现：

* 高频小步提交（几十条/天）
* 快速试错 → 快速回滚
* 文档 → 门禁 → 修复 → 再提交的循环

表达方式：

✅ “这种节奏更像是一种可持续试错机制，在 AI 参与下成本显著降低”
❌ 禁止泛泛谈 AI

---

====================
【方法论表达规则】
=========

方法论必须“延迟出现”，且满足：

* 来自多个阶段的重复模式
* 用归纳语气表达

❌ 禁止：

* 列条目（1/2/3）
* 直接写“方法论是……”

✅ 允许：

* “这些反复出现的动作，逐渐形成一种稳定偏好：……”
* “这一模式在后续被重复，说明该判断被保留下来”

---

====================
【语言风格】
======

整体风格：工程观察 + 轻叙事（不是报告）

推荐：

* “可以看到……”
* “更像是在……”
* “这一点在后续再次出现……”

避免：

* “本质是……”
* “核心在于……”
* “可以得出结论……”

---

====================
【结构要求】
======

1. 开头：声明（已给定）
2. 中间：阶段叙事（核心）
3. 结尾：

不要总结方法论条目
用一段“演进趋势”收束，例如：

“项目逐渐从‘能运行’，走向‘可约束、可修复、可持续演进’，而这种转变主要通过一系列小步试错与规则固化完成。”

---

====================
【输出要求】
======

* Markdown 格式
* 文件名：docs/60-human/STORY.md
* 覆盖写入
* 不输出任何额外解释
